server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/pplu24k/microservices-prueba
          search-paths: config-data
  application:
    name: config-service
     
                 
